title: ~
message: ~
description: Removing all development files except "tests" category in the package "vendor/package3"

config:
  vardot/composer-optimizer:
    clean:
      vendor/package3: ["!tests"]
